[package]
name = "zed-stylelint"
version = "3.0.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
semver = { version = "1", default-features = false }
zed_extension_api = "0.7.0"
